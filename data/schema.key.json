{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Key",
    "description": "An EC key.",
    "type": "object",
    "required": [ "overwrite", "ellipticCurve" ],
    "additionalProperties": false,
    "properties": {
        "$schema": {
            "type":"string",
            "description": "The schema to verify this document against."
        },
        "overwrite": {
            "type": [ "boolean" ],
            "description": "True to always generate a new key, false to preserve existing, if any."
        },
        "ellipticCurve": {
            "type": "string",
            "description": "The name of the elliptic curve to use for the key."
        }
    }
}
